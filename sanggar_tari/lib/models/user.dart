import 'package:flutter/material.dart';

class User {
  String id, name, alamat, noTelp, email;

  User(
      {required this.id,
      required this.name,
      required this.alamat,
      required this.noTelp,
      required this.email});
}
